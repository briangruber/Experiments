// STUB — replaced by the gameplay module owner.
export function createQuest() {
  const state = { title: 'A Strange Disturbance', body: 'Investigate the waters near Greenwake Island.', distance: 184, bearing: 0 };
  return { state, update() {}, dispose() {} };
}
