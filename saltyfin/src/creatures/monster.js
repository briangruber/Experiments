// STUB — replaced by the creature module owner.
import * as THREE from 'three';
export function createMonster() {
  const state = { position: new THREE.Vector3(0, -18, -160), surfaced: 0, alerted: false, distance: 184 };
  return { group: null, state, update() {}, applyEnv() {}, dispose() {} };
}
