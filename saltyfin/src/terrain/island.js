// STUB — replaced by the terrain module owner.
export function createIslands() {
  return {
    group: null, update() {}, applyEnv() {}, dispose() {},
    landHeight: () => -Infinity, isLand: () => false,
  };
}
